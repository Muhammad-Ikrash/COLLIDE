
spring.application.name=server-springboot

# Database Configuration
# If DB_POOLER environment variable is set, use PostgreSQL
# Otherwise, use H2 in-memory database for testing
spring.datasource.url=${DB_POOLER:jdbc:h2:mem:testdb}
spring.datasource.driver-class-name=${DB_DRIVER:org.h2.Driver}
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Disable data.sql since we use DataInitializer (programmatic approach is more reliable)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# H2 Console (for testing - access at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# PostgreSQL (when DB_POOLER is set)
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (Supabase)

jwt.secret=4Cso55zAZtpRmj/iMyOguBJXfbk/9X/iJ8aTOgGf/KgY8rj97f7rdCQMozAwuLoZJ+c/rW6rebNCRof6o1fm4g==
# Token expiration in milliseconds (86400000 = 24 hours)
# Note: Supabase tokens have their own expiration, this is just for validation
jwt.expiration=86400000